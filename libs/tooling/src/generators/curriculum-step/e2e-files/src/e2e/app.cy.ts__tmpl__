import {
  appTestName
} from '@class-materials/shared/e2e/helpers';

describe(appTestName(), () => {
  beforeEach(() => cy.visit('/'));

  it('should have app name and page title corresponding to project', () =>
    cy.checkAppNameAndPageTitle());
});
